---

- hosts: 172.31.96.108

  vars:
   root_password: 'root'

  tasks:
  - name: install mariadb
    apt:
     name: mariadb-server
     state: present
  - name: activation mariadb
    systemd:
     name: mariadb
     state: started
     enabled: yes
